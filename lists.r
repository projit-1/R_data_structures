list1 <- list("banana", "kala", "mala", "jala")
#list1
index_of_banana <- match("banana", list1)
index_of_banana

#To find out if a specified item is present in a list, use the %in% operator:

"kala" %in% list1
